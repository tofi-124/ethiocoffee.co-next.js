// Single source of truth for listing page sizes, shared by the list
// components, the sitemap, and scripts/validate-seo.ts so they can't drift.
export const INSIGHTS_PAGE_SIZE = 9
export const NEWS_PAGE_SIZE = 5
